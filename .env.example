# Optional overrides. Docker Compose works without copying this file.
# M3 will consume this value for JWT signing. Keep it empty until replacing it
# with Base64 for at least 32 random bytes; never commit a real value.
JWT_SIGNING_KEY_BASE64=
