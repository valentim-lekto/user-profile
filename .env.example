# Optional overrides. Docker Compose works without copying this file.
# The signing key remains empty here intentionally. If supplied, use Base64 for
# at least 32 random bytes and never commit the real value.
Jwt__Issuer=
Jwt__Audience=
Jwt__LifetimeMinutes=
Jwt__SigningKey=
